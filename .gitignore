# ─── Secrets & private data (NEVER commit) ────────────────────────────
config.ini
*.pem
cert.pem
key.pem
history.db
history.db-shm
history.db-wal
backups/

# ─── Python / virtualenv / build artefacts ────────────────────────────
venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/
.pytest_cache/
*.spec          # PyInstaller spec files (e.g. gen3-capture.spec)

# ─── Installer / packaging output ─────────────────────────────────────
Output/
dist/
build/
*.exe
*.zip

# ─── gen3-capture diagnostic: build output & captured data ────────────
gen3-capture/build/
gen3-capture/dist/
gen3-capture/Gen3 Capture/
gen3-capture/capture_*.bin
gen3-capture/capture_*.log
gen3-capture/gen3_config.ini

# ─── Website (kept off GitHub — lives on the marketing host only) ─────
website/

# ─── GivEnergy API Catalogue reference tooling (separate project) ─────
serve.py
index.html
catalogue.json
build_catalogue.py
build_local_docs.py
givenergy-api-docs.html
givenergy-api-v1.openapi.yaml
givenergy-local-modbus.json

# ─── User guide (local only — screenshots + Word doc, not for repo) ──
user-guide/

# ─── Local planning docs (not for public repo) ───────────────────────
BACKLOG.md
RELEASE_PROCESS.md
PROJECT_MEMORY.md
PROMPT-model-detection-audit.md
V*_KICKOFF.md

# ─── Reference / research material (large binaries, vendor copyright) ─
# PDFs are gitignored (vendor copyright + size); text files (models.md, INDEX.md)
# are also local-only to keep the repo focused on shippable code.
reference/

# ─── Editor / OS / local agent ────────────────────────────────────────
.claude/
.vscode/
.idea/
.DS_Store
Thumbs.db
